<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= user ? 'Edit User' : 'Add User' %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <style>
    body {
      background: linear-gradient(120deg, #f0f4f8 0%, #dbeafe 100%);
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', 'Microsoft YaHei', Arial, sans-serif;
    }
    .admin-edit-user-container {
      background: #fff;
      padding: 2.5em 2em;
      border-radius: 14px;
      box-shadow: 0 4px 24px rgba(37,99,235,0.10);
      min-width: 500px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .admin-edit-user-container h2 {
      color: #2563eb;
      margin-bottom: 1.5em;
      font-size: 1.7rem;
      letter-spacing: 1px;
    }
    form {
      width: 100%;
      display: flex;
      flex-direction: column;
    }
    label {
      margin-top: 1em;
      margin-bottom: 0.4em;
      color: #374151;
      font-weight: 500;
    }
    input[type="text"],
    input[type="email"],
    input[type="password"] {
      padding: 0.7em;
      border: 1px solid #cbd5e1;
      border-radius: 8px;
      font-size: 1rem;
      outline: none;
      transition: border-color 0.3s;
    }
    input:focus {
      border-color: #2563eb;
    }
    button[type="submit"] {
      margin-top: 1.8em;
      background: #2563eb;
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 0.7em;
      font-size: 1rem;
      cursor: pointer;
      font-weight: 500;
      transition: background 0.2s, transform 0.2s;
    }
    button[type="submit"]:hover {
      background: #1e40af;
      transform: translateY(-2px) scale(1.04);
    }
    a {
      margin-top: 1.5em;
      color: #2563eb;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.2s;
    }
    a:hover {
      color: #1e40af;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="admin-edit-user-container">
    <h2><%= user ? 'Edit User' : 'Add User' %></h2>
    <form action="<%= user ? '/admin/edit/' + user.user_id : '/admin/add' %>" method="post">
      <label>Username:</label>
      <input type="text" name="user_name" value="<%= user ? user.user_name : '' %>" required>
      <label>Email:</label>
      <input type="email" name="email" value="<%= user ? user.email : '' %>" required <%= user ? 'readonly' : '' %>>
      <label>Avatar URL:</label>
      <input type="text" name="avatar_url" value="<%= user ? user.avatar_url : '' %>">
      <label>Password:</label>
      <input type="password" name="password">
      <button type="submit">Save</button>
    </form>
    <a href="/admin/users">Back to User List</a>
  </div>
</body>
</html>
